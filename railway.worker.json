{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5,
    "startCommand": "sh -c \"until php artisan db:show > /dev/null 2>&1; do echo 'Waiting for database...'; sleep 2; done && php artisan queue:work --tries=3 --max-time=3600 --sleep=3\"",
    "sleepApplication": false
  }
}
